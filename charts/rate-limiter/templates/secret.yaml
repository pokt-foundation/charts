{{- if .Values.env.normal -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ template "pokt-rate-limiter.fullname" . }}
    chart: {{ template "pokt-rate-limiter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $val := .Values.env.normal }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
{{- end }}