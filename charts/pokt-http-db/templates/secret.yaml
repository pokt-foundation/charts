{{- if .Values.env.secret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-vars
  labels:
    app: {{ template "pokt-http-db.fullname" . }}
    chart: {{ template "pokt-http-db.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $val := .Values.env.secret }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
{{- end }}
