apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "aptos.fullname" . }}
  labels:
    {{- include "aptos.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "aptos.selectorLabels" . | nindent 6 }}
  serviceName: {{ include "aptos.fullname" . }}
  template:
    metadata:
      annotations:
        checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "aptos.selectorLabels" . | nindent 8 }}
        in_rotation: {{ .Values.aptos.in_rotation | quote }}
        
    spec:
      terminationGracePeriodSeconds: 300
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsUser: 0
      initContainers:
        - name: {{ .Chart.Name }}-init-node
          image: debian:11-slim
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ['sh', '-c']
          args:
          - /scripts/init-node.sh
          volumeMounts:
            - name: scripts
              mountPath: /scripts/
            - name: configs
              mountPath: /configs/
            - name: home
              mountPath: {{ .Values.aptos.home }}

      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/opt/aptos/bin/aptos-node"]
          args: 
          - "-f"
          - "{{ .Values.aptos.home }}/node.yaml"
          livenessProbe:
            tcpSocket:
              port: {{ .Values.aptos.ports.api }}
            initialDelaySeconds: 60
            periodSeconds: 30
          ports:
            - name: api
              containerPort: {{ .Values.aptos.ports.api }}
              protocol: TCP
            - name: consensus
              containerPort: {{ .Values.aptos.ports.consensus }}
              protocol: TCP
              hostPort: {{ .Values.aptos.ports.consensus }}
          volumeMounts:
            - name: home
              mountPath: {{ .Values.aptos.home }}
          resources:
            {{- toYaml .Values.aptos.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: scripts
          configMap:
            name: {{ include "aptos.fullname" . }}-scripts
            defaultMode: 0777
        - name: configs
          configMap:
            name: {{ include "aptos.fullname" . }}-configs
{{- if and .Values.persistence.enabled .Values.persistence.existingClaim  }}
        - name: home
          persistentVolumeClaim:
{{- with .Values.persistence.existingClaim }}
            claimName: {{ tpl . $ }}
{{- end }}
{{- else if and .Values.persistence.enabled .Values.persistence.hostPath }}
        - name: home
          hostPath:
            path: {{ .Values.persistence.hostPath.path }}
            type: {{ .Values.persistence.hostPath.type }}
{{- else if not .Values.persistence.enabled }}
        - name: home
          emptyDir: {}
{{- else if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
  volumeClaimTemplates:
    - metadata:
        name: home
      {{- with .Values.persistence.annotations }}
        annotations:
        {{- range $key, $value := . }}
            {{ $key }}: {{ $value }}
        {{- end }}
      {{- end }}
      spec:
        accessModes:
        {{- range .Values.persistence.accessModes }}
          - {{ . | quote }}
        {{- end }}
        storageClassName: {{ .Values.persistence.storageClassName }}
        resources:
          requests:
            storage: {{ .Values.persistence.size | quote }}
{{- end }}