apiVersion: v1
kind: Service
metadata:
  name: {{ include "pokt-txdb-http-db.fullname" . }}
  labels:
    {{- include "pokt-txdb-http-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "pokt-txdb-http-db.selectorLabels" . | nindent 4 }}
