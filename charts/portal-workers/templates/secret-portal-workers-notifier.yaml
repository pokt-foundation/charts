{{- if .Values.notifier.env.secret -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-notifier-vars
  labels:
    app: {{ template "pokt-portal-workers.fullname" . }}
    chart: {{ template "pokt-portal-workers.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $val := .Values.notifier.env.secret }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
{{- end }}
