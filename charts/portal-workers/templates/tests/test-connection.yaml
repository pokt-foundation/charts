apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokt-portal-workers.fullname" . }}-test-connection"
  labels:
    {{- include "pokt-portal-workers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokt-portal-workers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
