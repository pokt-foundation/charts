---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ethereum.fullname" . }}-test-noop"
  labels:
    {{- include "ethereum.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - args:
        - passed
      command:
        - echo
      image: busybox
      name: noop-test
  restartPolicy: Never
