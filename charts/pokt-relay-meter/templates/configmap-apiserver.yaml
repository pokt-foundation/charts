apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "pokt-relay-meter.fullname" . }}-apiserver
  labels:
    app: {{ template "pokt-relay-meter.fullname" . }}
    chart: {{ template "pokt-relay-meter.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
{{ toYaml .Values.apiserver.configurationFile | indent 2 }}
