apiVersion: v1
kind: Service
metadata:
  name: {{ include "pokt-middleware.fullname" . }}
  labels:
    {{- include "pokt-middleware.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pokt-middleware.selectorLabels" . | nindent 4 }}
