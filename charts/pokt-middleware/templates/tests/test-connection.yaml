apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokt-middleware.fullname" . }}-test-connection"
  labels:
    {{- include "pokt-middleware.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokt-middleware.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
