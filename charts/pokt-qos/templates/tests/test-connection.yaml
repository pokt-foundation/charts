apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokt-qos.fullname" . }}-test-connection"
  labels:
    {{- include "pokt-qos.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pokt-qos.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
