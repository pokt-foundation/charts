{{- if .Values.externalSecrets.enabled -}}
kind: ExternalSecret
apiVersion: external-secrets.io/v1beta1
metadata:
  name: {{ include "portal-middleware.fullname" . }}
spec:
  refreshInterval: "60s"
  secretStoreRef:
    name: {{ include "portal-middleware.fullname" . }}-ss
    kind: SecretStore
  target:
    name: {{ include "portal-middleware.fullname" . }}-es
    deletionPolicy: "Delete"

  dataFrom:
  - extract:
      key: {{ .Values.externalSecrets.key}}
      decodingStrategy: {{ .Values.externalSecrets.decodingStrategy }}
{{- end -}}
