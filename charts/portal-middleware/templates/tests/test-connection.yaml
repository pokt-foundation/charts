apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portal-middleware.fullname" . }}-test-connection"
  labels:
    {{- include "portal-middleware.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portal-middleware.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
