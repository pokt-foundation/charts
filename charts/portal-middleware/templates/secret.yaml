apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-vars
  labels:
    app: {{ template "portal-middleware.fullname" . }}
    chart: {{ template "portal-middleware.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $val := .Values.env.secret }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}
