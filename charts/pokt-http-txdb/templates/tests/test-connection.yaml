apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pokt-http-txdb.fullname" . }}-test-connection"
  labels:
    {{- include "pokt-http-txdb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: nc
      image: busybox
      command: ['nc']
      args:
        - '-zv'
        - '{{ include "pokt-http-txdb.fullname" . }}'
        - '{{ .Values.service.port }}'
  restartPolicy: Never
