apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "klaytn.fullname" . }}-test-connection"
  labels:
    {{- include "klaytn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "klaytn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
