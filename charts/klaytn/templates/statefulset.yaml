apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "klaytn.fullname" . }}
  labels:
    {{- include "klaytn.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "klaytn.selectorLabels" . | nindent 6 }}
  serviceName: {{ include "klaytn.fullname" . }}
  template:
    metadata:
      annotations:
        checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "klaytn.selectorLabels" . | nindent 8 }}
        in_rotation: {{ .Values.klaytn.in_rotation | quote }}
        
    spec:
      terminationGracePeriodSeconds: 300
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsUser: 0
      initContainers:
        - name: {{ .Chart.Name }}-init-node
          image: debian:11-slim
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ['sh', '-c']
          args:
          - /scripts/init-node.sh
          volumeMounts:
            - name: scripts
              mountPath: /scripts/
            - name: configs
              mountPath: /configs/
            - name: datadir
              mountPath: {{ .Values.klaytn.datadir }}

      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["kend"]
          args: 
          - start-docker
          livenessProbe:
            tcpSocket:
              port: {{ .Values.klaytn.ports.rpc }}
            initialDelaySeconds: 60
            periodSeconds: 30
          ports:
            - name: rpc
              containerPort: {{ .Values.klaytn.ports.rpc }}
              protocol: TCP
            - name: consensus
              containerPort: {{ .Values.klaytn.ports.consensus }}
              protocol: TCP
              hostPort: {{ .Values.klaytn.ports.consensus }}
          volumeMounts:
            - name: datadir
              mountPath: {{ .Values.klaytn.datadir }}
          resources:
            {{- toYaml .Values.klaytn.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: scripts
          configMap:
            name: {{ include "klaytn.fullname" . }}-scripts
            defaultMode: 0777
        - name: configs
          configMap:
            name: {{ include "klaytn.fullname" . }}-configs
{{- if and .Values.persistence.enabled .Values.persistence.existingClaim  }}
        - name: datadir
          persistentVolumeClaim:
{{- with .Values.persistence.existingClaim }}
            claimName: {{ tpl . $ }}
{{- end }}
{{- else if and .Values.persistence.enabled .Values.persistence.hostPath }}
        - name: datadir
          hostPath:
            path: {{ .Values.persistence.hostPath.path }}
            type: {{ .Values.persistence.hostPath.type }}
{{- else if not .Values.persistence.enabled }}
        - name: datadir
          emptyDir: {}
{{- else if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
  volumeClaimTemplates:
    - metadata:
        name: datadir
      {{- with .Values.persistence.annotations }}
        annotations:
        {{- range $key, $value := . }}
            {{ $key }}: {{ $value }}
        {{- end }}
      {{- end }}
      spec:
        accessModes:
        {{- range .Values.persistence.accessModes }}
          - {{ . | quote }}
        {{- end }}
        storageClassName: {{ .Values.persistence.storageClassName }}
        resources:
          requests:
            storage: {{ .Values.persistence.size | quote }}
{{- end }}