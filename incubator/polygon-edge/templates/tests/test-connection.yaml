apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polygon-edge.fullname" . }}-test-connection"
  labels:
    {{- include "polygon-edge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polygon-edge.fullname" . }}:{{ .Values.jsonrpc.service.port }}']
  restartPolicy: Never
